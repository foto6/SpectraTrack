# SpectraTrack v0.2.2

CLI hotfix.

## Fixed
- `--conf 0.20` is no longer incorrectly interpreted as an abbreviated `--config 0.20` by the preset pre-parser.
- Argument abbreviation is disabled for predictable command-line behavior.
- Includes the v0.2.1 source-FPS recording fix.

## Assets
- **SpectraTrack-Windows-x64.zip**
- **SpectraTrack-Android.apk**
- **SHA256SUMS.txt**

Model weights remain separate.
